<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CablaggioService" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CablaggioService" parent="Base.Theme.CablaggioService" />

    <style name="RoundedCornersDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@drawable/dialog_rounded</item>
        <item name="android:buttonBarStyle">@style/CustomButtonBar</item>
    </style>

    <style name="CustomButtonBar" parent="Widget.AppCompat.ButtonBar.AlertDialog">
        <!-- Personalizza il padding tra i pulsanti -->
        <item name="android:paddingStart">-10dp</item>
        <item name="android:paddingEnd">-10dp</item>
        <item name="android:layout_marginRight">0dp</item>
        <item name="android:layout_marginLeft">0dp</item>
    </style>
</resources>